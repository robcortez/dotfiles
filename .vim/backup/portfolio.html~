{% extends "base.html" %}
{% block content %}

<div class="jumbotron jumbotron-sm">
    <div class="container">
        <div class="row">
            <div class="col-sm-12 col-lg-12">
                <h1 class="h1">
                    Portfolio <small>Check out some of my work.</small></h1>
            </div>
        </div>
    </div>
</div>
<div class="container">
    <div class="col-md-offset-2 col-md-8">
        <div class="well">
            <a href="http://instagram.com/tashymariemakeup" target="_blank" class="no-hover">
                <h3 class="text-center insta-banner">Follow me on <img src="{{url_for('static', filename='img/social/ig-big.png')}}" width="40"> Instagram for more photos</h2>
            </a>
        </div>
    </div>
</div>
<div class="container" class="top-buffer">
    {% if photos %}
    {% for p in photos %}
    {% if loop.index is divisibleby 7 %}
    </div>
    <div class="container">
    {% endif %}
    <div class="col-md-2">
         <a href="{{p.url}}" title="{{p.caption}}" class="fancybox" rel="group1"><img src="{{p.url}}" class="img-thumbnail"></a>
    </div>
    {% endfor %}
    {% endif %}
</div>


{% endblock %}

