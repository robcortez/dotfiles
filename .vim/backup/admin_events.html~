{% extends "base.html" %}

{% block content %}

<ul class="nav nav-tabs nav-justified">
  <li role="presentation"><a href="/admin">Dashboard</a></li>
  <li role="presentation"><a href="/admin/users">Users</a></li>
  <li role="presentation" class="active"><a href="/admin/events">Events</a></li>
  <li role="presentation"><a href="#">Scores</a></li>
</ul>

{% for field in form.errors %}
{% for error in form.errors[field] %}
    <div class="alert alert-error">
        <strong>Error!</strong> {{error}}
    </div>
{% endfor %}
{% endfor %}
<div class="row">

    <div class="form-add-event">
        <h2>Create event</h2>
        <form action="" method="post" class="form-inline">
            <div class="form-group">
                <label for="eventName">Event name</label>
                {{ form.event_name(class_="form-control",id="eventName") }}
            </div>
            <div class="form-group">
                <label for="datetimepicker">Date/time</label>
                {{ form.datetimepicker(class_="form-control",id="datetimepicker") }}
            </div>
            <div class="form-group">
                <label for="price">Price</label>
                {{ form.price(class_="form-control",id="price", size="5") }}
            </div>
            <button type="submit" class="btn btn-success">Create</button>
        </form>
    </div>
</div>

<table class="table table-striped table-events">
    <thead>
        <tr>
            <td>Event</td>
            <td>Date/Time</td>
            <td>Price</td>
            <td>Registered</td>
        </tr>
    </thead>
    <tbody>
    {% for e in events %}
        <tr>
            <td>{{e.event_name}}</td>
            <td>{{e.datetime}}</td>
            <td>{{e.price}}</td>
            <td></td>
        </tr>
    {% endfor %}
    </tbody>
</table>



    <script src="/static/js/moment.min.js"></script>
    <script src="/static/js/bootstrap-datetimepicker.min.js"></script>
    <script type="text/javascript">
        $(function () {
            $('#datetimepicker').datetimepicker();
        });
    </script>
{% endblock %}
